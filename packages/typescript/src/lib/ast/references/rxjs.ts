import { tsReference } from '../nodes/reference';

export const observable = tsReference.factory('Observable', 'rxjs');
export const filter = tsReference.factory('filter', 'rxjs');
export const map = tsReference.factory('map', 'rxjs');
