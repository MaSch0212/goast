import { ktReferenceFactory } from '../ast';

export const pattern = ktReferenceFactory('Pattern', 'jakarta.validation.constraints');
export const valid = ktReferenceFactory('Valid', 'jakarta.validation');
